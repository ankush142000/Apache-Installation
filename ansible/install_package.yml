---
- name: Install and configure Apache
  hosts: all
  become: yes
  tasks:
   - name: Install Apache Package
     yum:
       name: httpd
       state: present
   - name: Ensure Apache service is running
     service:
       name: httpd
       state: started
       enabled: yes  
